# CLAUDE.md

Guidance for Claude Code (or any agent) working in this repository.

## What this is

A personal, single-user Indian stock market app: nightly NSE/BSE scanning, strategy backtesting with a promotion gate, live pick tracking, and a virtual paper-trading playground. Full requirements: **`docs/PROJECT_BRIEF.md`** — read it before making any decision that isn't purely mechanical. The implementation plan (folder structure, schemas, phase sequencing, and the research corrections to the brief) is in `docs/adr/` and `docs/BUILD_PLAN.md`; verified external endpoints are in `docs/data-sources.md`.

## Ground rules (from the brief, do not relitigate without asking)

- **Single user.** No multi-tenancy, no sign-up flow.
- **Free data sources**, always behind a provider adapter (`stk.providers.base`). Never let ingest/domain/API code import a concrete provider class directly — go through `stk.providers.registry`.
- **IST everywhere, INR with lakh/crore formatting.** Use `stk.core.time` and `stk.core.money`, not ad-hoc datetime/float handling.
- **Never execute AI-generated code.** Strategies are DSL JSON, validated against a schema and interpreted.
- **Fail loudly.** A missing/malformed corporate action, an unexpected content-type, a missing trading day — these raise or exit non-zero. Never silently default to a "safe-looking" no-op; see `stk.ingest.corpactions`'s module docstring for the concrete failure mode this prevents (a missed bonus/split silently corrupting a price series).
- **Raw bytes are sacred.** Every fetched response is persisted under `data/raw/` before parsing (`stk.ingest.raw_store`), content-addressed by sha256, so re-parsing never needs the network.
- **Parquet writes are overwrite-by-partition, never append** (`stk.store.parquet.writer`). This is the idempotency mechanism for the whole ingest pipeline — do not add an append path.
- **`job_runs` is observability, not a lock.** Re-running any ingest job for the same date must always be safe; it never checks "did this already succeed" before running again.

## Repo layout

```
backend/src/stk/
  config/     settings.py (pydantic-settings, layered YAML+env), costs.py (dated rate schedule)
  core/       time.py (IST), money.py (Decimal/lakh-crore), http.py (content validation), errors.py
  providers/  base.py (5 ABCs), registry.py, nse/, bse/, yfinance/, broker/ (phase-8 placeholder)
  ingest/     daily.py, backfill.py (orchestrators), normalise.py, corpactions.py, assertions.py, jobs.py
  store/      parquet/ (schema, writer), db/ (SQLite migrations + engine), duck.py
  domain/     universe.py, calendar.py -- PURE functions, no I/O
  cli/        typer app `stk`: db, ingest, backfill, doctor
config/       defaults.yaml, env/{local,prod}.yaml, costs.yaml, universe.yaml, horizons.yaml
tests/        unit/, integration/, fixtures/ (real, trimmed exchange responses)
docs/         PROJECT_BRIEF.md, data-sources.md, adr/, runbook.md (phase 8), ORACLE_VM_SETUP.md
```

## Working here

- **Package manager: `uv`.** `uv sync --all-groups`, `uv run pytest`, `uv run stk ...`. The workspace root `pyproject.toml` has `backend` as a member.
- **Tests:** `uv run pytest` (live-network tests excluded by default via the `live` marker; run `uv run pytest -m live` deliberately when verifying an endpoint is still reachable in its documented shape). Fixtures in `tests/fixtures/` are **real, trimmed exchange responses**, not synthetic data — prefer extending these over hand-writing new synthetic fixtures when testing a parser.
- **Lint/type-check:** `uv run ruff check backend/src tests scripts` and `uv run mypy backend/src/stk`. Both must be clean before considering a change done. `domain/` and `ingest/` are mypy-strict (`disallow_untyped_defs`).
- **Architecture contracts:** `uv run lint-imports` enforces, as code, the rules this file states in prose — `ingest/`/`domain/`/`cli/`/`store/` may not *directly* import a concrete provider module (going through `providers.registry` is the sanctioned path), `domain/` imports no I/O or storage library, and the `cli > ingest > store > config > domain > core` layering holds. Contracts live in the root `pyproject.toml` under `[tool.importlinter]`. Must be clean alongside ruff and mypy. Note `providers/` imports `ingest.normalise` (the shared bhavcopy parsers) while `ingest/` imports `providers.base`/`registry`, so those two packages are deliberately outside the layering contract.
- **CI:** `.github/workflows/ci.yml` runs ruff, mypy, `lint-imports` and `pytest -m "not live"` on every push/PR. It must stay network-free. The live-endpoint suite is a separate `workflow_dispatch`-only job — endpoint drift is checked deliberately, never as a blocking gate.
- **Config:** anything a human would tune (cost rates, liquidity thresholds, horizon windows) lives in committed YAML under `config/`. Anything secret lives in `.env` (see `.env.example`), never committed.
- **Cost rates in `config/costs.yaml` are explicitly flagged as unverified against primary NSE/SEBI circulars** — sourced from broker-published schedules. Do not remove that warning without actually verifying against a primary source.
- When a data-source URL or format needs re-checking, update `docs/data-sources.md` in the same change — it is meant to be the living, re-verified record, not a one-time research dump.

## Current status (update this section as phases complete)

- **Phase 0 (repo/config/tooling):** done.
- **Phase 1 (data pipeline + store):** substrate complete and tested — config system, canonical schemas, SQLite migrations, the atomic parquet writer, the corporate-action parser, live-verified daily ingest for **both exchanges** (`stk ingest daily` now runs NSE and BSE, `stk doctor`), and live-verified backfill for both (`stk backfill prices --exchange NSE|BSE`), both now reaching **2010-01-04**. Both exchanges have automatic source selection across two confirmed price archives each (see `docs/adr/0003-historical-price-source.md`): NSE via `get_nse_price_provider_for_date`, BSE via `get_bse_price_provider_for_date` (the previously-deferred spike step 4 is now resolved — BSE's legacy `EQ*.CSV.ZIP` archive is confirmed live back to 2010-01-04, with a clean handoff to UDiFF at 2024-07-08, no gap). The NSE spike also surfaced a real NSE data-quality bug (some historical dates serve mislabeled content) — guarded against by `assert_bars_match_requested_date`. BSE's "silent 200" trap (an HTML SPA shell instead of a 404 for any non-trading/invalid date) is confirmed live on both its UDiFF and legacy URL families — handled uniformly by treating any 200-with-`text/html` response as `DataNotPublished`. The liquidity filter (`stk.ingest.liquidity`, `stk ingest liquidity`) is implemented: DuckDB computes trailing-20-trading-day median turnover/volume/trades and listed-days per symbol directly from `bars_daily` parquet (no re-fetch), `domain.universe.is_liquid` applies the pure threshold rule, results land in `features/liquidity_daily/` parquet and `universe_current` SQLite (thresholds in `config/universe.yaml`, loaded via `stk.config.universe`). Security-master providers (`stk.providers.nse.master.NseSecurityMasterProvider`, `stk.providers.bse.master.BseSecurityMasterProvider`) and the ISIN-keyed merge into `securities`/`listings`/`symbol_history` (`stk.ingest.master.ingest_security_master`, `stk ingest master`) are live-verified and implemented, so `universe_current` now resolves real `security_id`s once `stk ingest master` has been run at least once. Rename detection (a symbol change on the same exchange/ISIN) is tracked via `symbol_history`. **Documented, deferred limitation:** suspension/delisting detection (absence across N consecutive snapshots) is not implemented — a security missing from today's snapshot is left at its previous status, never silently flipped; see `ingest/master.py`'s module docstring.

A live `NseCorporateActionsProvider` is implemented and wired up (`stk.ingest.corpactions.ingest_corporate_actions`, `stk ingest corpactions`): fetches from NSE's `corporates-corporateActions` API (confirmed live 2026-09-18 to need only a browser UA — **no cookie handshake required in practice**, correcting an earlier assumption in `docs/data-sources.md`), parses subjects via the existing `parse_subject`, and upserts into `corporate_actions` keyed on `(source, source_hash)`. Honors `ingest.fail_on_unparsed_corp_action` — an unrecognised subject aborts the run rather than silently no-opping.

A live `NseFundamentalsProvider` is implemented (`stk.ingest.fundamentals.ingest_fundamentals_for_security`, `stk ingest fundamentals SYMBOL --isin ISIN`). **Important correction to the original build plan's research:** `resultDetailedDataLink` (assumed to carry pre-parsed financial figures) was found empty on every live-sampled row — this provider gives genuine, official, point-in-time **filing metadata** (broadcast date, audited/consolidated flags, an XBRL document link), stored as `statement_type="meta"`, not parsed income/balance/cashflow line items. Full XBRL parsing is unimplemented and deliberately deferred (a real, substantial separate scope) — see `docs/data-sources.md`'s fundamentals section. Only `Period.QUARTERLY`/`ANNUAL` are supported; `HALF_YEARLY`/`TTM` raise `NotSupportedError` since their API query values weren't reliably confirmed. The `yfinance` fallback provider named in `config/defaults.yaml` remains unimplemented.

All four previously-open Phase 1 items are now addressed (BSE history, liquidity filter, security master, corporate actions, fundamentals-metadata) — remaining known gaps are narrow and explicitly documented above (BSE suspension/delisting detection, XBRL line-item parsing), not silently missing.

- **Phase 1.5 (closing the gaps between Phase 1's code and Phase 1's build plan):** done. An audit found four build-plan subsystems absent and three more present only as uncalled scaffolding. All are now implemented:
  - **Corporate-action adjustments** (`stk.ingest.adjustments`, `stk ingest adjustments`, wired into `stk ingest daily` as step 6). Cumulative back-adjustment factors composed in `Decimal` from `corporate_actions`, written to `adjustment_factors/` and materialised into `bars_daily_adjusted/`. **`bars_daily` is never rewritten.** Three things this gets right on purpose, each with its own test: a bar *on* an ex-date is unadjusted (the classic off-by-one); a republished action (same economics, new `source_hash` — which the schema keeps by design) is applied once, not twice; and a factor announced under a renamed symbol still reaches bars written under the old one, via `symbol_history`. `turnover` is never scaled — rupees traded is a fact, and it is conserved anyway. An action that cannot be turned into a factor is excluded, **counted, and marks the run `degraded`** (a new `JobRunHandle.degraded`), never silently smoothed over.
  - **Trading calendar** (`stk.providers.nse.calendar`, `stk.ingest.calendar`, `stk ingest calendar`). `trading_calendar` is populated from NSE's holiday master, which a live probe showed reaches back to **2011** (the build plan assumed current-year only), plus an `observed_bars` derivation for what the lake itself proves. Precedence: an observed row never degrades an authoritative one. `ingest.daily` now consults it before fetching and a known holiday costs **zero HTTP requests** — but the read is deliberately tri-state: **no calendar row means unknown, and unknown is fetched**, never skipped.
  - **DuckDB query layer** (`stk.store.duck`, `store/queries/{prices,screen,coverage}.sql`). `duck.py` was a 0-byte file. Every parquet read outside `store/` now goes through it. Datasets not yet ingested register as **empty typed views, not errors**. `ingest.liquidity`'s ad-hoc inline DuckDB call was refactored onto it with its tests unchanged. Note `sqlite_scanner` is not bundled with DuckDB and `ATTACH … TYPE SQLITE` would download it — so there is a logged Arrow fallback, and tests/CI force it so no run can reach the network for an extension.
  - **`_manifests`** — `upsert_partition` now writes a row-count + sha256 sidecar per partition, and `stk doctor` verifies it. **Caveat:** partitions are *data*-idempotent, not *byte*-idempotent, because `ingested_at` is a fresh timestamp per run. The build plan's "re-run → identical sha256" acceptance line was never achievable with `ingested_at` in the canonical schema; row data is identical and the manifest is rewritten alongside the file, so the corruption check is unaffected.
  - **Hardened `stk doctor`** — checks now live in `stk.ingest.health` as plain testable functions: calendar-vs-parquet gaps, a 90-day job-run window, stale symbols (counted in *trading* days), manifest verification, adjusted-series freshness, plus `--check-endpoints` for deliberate live probing. Each check stays quiet when it cannot judge honestly (e.g. history older than the lake is not a gap).
  - **`indices_daily` + NSE UDiFF + yfinance + CI.** Index closes (`stk ingest indices`) give Phase 2 its benchmark. `NseUdiffProvider` is registered as the ISIN-bearing companion but is **never** selected automatically — it lacks delivery data, which `sec_bhavdata_full` has. yfinance is implemented but **disabled by default**; the registry raises `ConfigError` unless `providers.enable_yfinance_fallback` is set, so it cannot drift onto the critical path. `.github/workflows/ci.yml` runs ruff/mypy/`lint-imports`/pytest.

  **New constraints Phase 2 must plan around, both live-verified:** the index archive starts **2012-02-21**, roughly two years *after* the price lake begins — a backtest starting in 2010 has prices but no benchmark for its first two years. And NSE has renamed the Nifty twice inside the range (`S&P CNX Nifty` → `CNX Nifty` → `Nifty 50`), so a benchmark series must key on the canonical `index_code`, never the printed name.

- **Phase 2 (backtest engine):** done. Pure logic is in `domain/` — `costs.py` (Decimal `compute_costs` + separate `dp_charge`; GST only on the taxable subset), `slippage.py` (liquidity-tiered bps + participation cap), `fills.py` (circuit-lock *heuristic*, since historical bands don't exist), `metrics.py`, `walkforward.py`. `stk.backtest` holds the engine (`engine.py`: exits → entries → mark → signal each day; signals fill at T+1 open; a gap through a stop fills at the open; stop beats target in one bar), `view.py` (`PointInTimeView`, the *only* data handle a strategy gets — slices by date and re-asserts, raising `LookAheadError`), `data.py`, `walkforward.py`, `runs.py` (migration `0002_backtest.sql`). `stk costs quote` and `stk backtest list|show` exist; **`stk backtest run` arrives with the DSL in Phase 3** (the engine takes any `Strategy` protocol object). Things this phase got right on purpose, each with a test: the no-look-ahead proof compares *decisions* on a poisoned-future copy (comparing settled trades alone let a one-row leak through — found by mutation-testing it); cash is conserved exactly (`final equity == capital + Σ net_pnl`); an uncovered benchmark is `benchmark_missing`, never back-filled. **Fixed a real bug on the way:** `is_liquid` rejected every NSE symbol before 2019-10 because the legacy bhavcopy has no trades column (median trades was null → "illiquid"); an *unreported* trades count now skips only that check. **Cost rates:** NSE's 2026-03-01 change is now verified against NSE/FA/73061 and *corrected* (transaction charge is Rs 306.99/crore, not 307 — IPFT is a separate line); the rest of `costs.yaml` is still unverified, see the ledger in `docs/data-sources.md`. Backtests are **NSE-only in v1** (BSE's legacy feed keys on scrip codes, not tickers, until 2024-07-08).
- **Phase 3 (features, XBRL, DSL, seed strategies, promotion gate):** done. `domain/indicators.py` (past-only; `test_indicators.py` proves it by truncation-invariance and a poisoned future, mutation-checked), `domain/dsl/` (`catalogue.py` closed indicator set, `model.py` strict pydantic, `validate.py`, `evaluate.py` interpreter — never executes spec content; `config/strategy.schema.json` is committed and drift-tested), ten seed specs in `config/strategies/*.json` (the brief lists ten — the build plan's "twelve" was a miscount), `domain/gate.py` + `config/promotion.yaml` (verdicts `pass`/`fail`/`insufficient_evidence`; insufficient leaves a strategy `candidate`, never `rejected`), `strategies/` (`repo.py` immutable versions + audited status events, `runner.py`, `promotion.py`, `dsl_strategy.py`), migrations `0003_fundamentals` and `0004_strategies`. **XBRL** (`ingest/xbrl.py`, `ingest/fundamentals_xbrl.py`, `ingest/fundamentals_metrics.py`, `stk ingest xbrl|fundamentals-sweep`): built against two REAL filings after a live probe — **context IDs carry the meaning, the printed dates cannot be trusted** (`FourD` prints `OneD`'s dates but holds year-to-date values), quarterly filings have no balance sheet, `DebtEquityRatio` reads 0.00 for a heavily indebted company so it is never used; see `docs/data-sources.md`. Only `parsed` filings feed metrics; `partial` ones stay out and stay visible. New commands: `stk strategies validate|schema|seed|list|explain|promote`, `stk backtest run`. Deviations from the plan are in `docs/BUILD_PLAN.md` "Amendments" (indicators computed on the fly, no `fundamentals_metrics` table). **Two real bugs found by running against live data, not synthetic fixtures — worth remembering why synthetic tests alone were not enough:** (1) `assert_bars_sane` aborted whole days over auxiliary-series rows like WIPRO's `T0` stub (open=high=low=269.00 but close=269.65), which blocked ~1/3 of real trading days; the OHLC-ordering check is now fatal only for equity series (`OHLC_STRICT_SERIES`) and a counted, logged anomaly otherwise, rows still stored. (2) `bars_daily_adjusted` holds several series per symbol-day (238 duplicate rows in 49 days), so the backtest panel now selects ONE tradeable series (`tradeable_series` in `config/backtest.yaml`, EQ > BE > BZ) and `prepare_bars` raises on any duplicate; the panel also once omitted `cumulative_price_factor`, so `close_raw` silently equalled the adjusted close. **Known gaps, stated plainly:** the brief's long-term seed names promoter holding, pledge % and P/E vs the stock's own history — none are implemented (no shareholding provider; not enough EPS history), so that strategy uses a plain P/E band and is always `approx`. The NSE filings API history is shallow (Reliance annual listing began FY2021), so 3-year CAGRs exist only from ~FY2024. Only two XBRL template variants have been observed; a real `fundamentals-sweep` + `xbrl` run will reveal more `partial`/`unsupported_format` — read those counts before trusting fundamentals-based strategies.
- **Phase 4 (picks, API, dashboard):** done. `strategies/scan.py` (live/decaying strategies only, idempotent, scores normalised to 0-100), `strategies/tracking.py` + `domain/tracking.py` (mark to market, stop/target/time on the same rules as the backtest, ratios of ADJUSTED bars so a bonus can't fake a crash, net of round-trip costs, picks that never get a bar are voided), `strategies/stats.py` (OUT-OF-SAMPLE backtest numbers beside live ones; only the system may move live -> decaying), `stk/api/` (FastAPI, bearer tokens with only a hash stored, `deps.py`/`services.py`/`playground_routes.py`; the stale banner is judged on the DATA so a timer that never fired still warns; `approve` refuses anything that has not passed the gate), `web/` (React + Vite + TS + TanStack Query; styles taken from `docs/design`; types GENERATED from `web/openapi.json`, with a Python drift test and a CI job that regenerates and diffs). Migration `0005_picks`. **The panel loader has a load-time liquidity floor** (`panel_min_peak_turnover_inr`): a symbol is loaded only if its best day reached the floor -- provably cannot drop a stock that could pass a median filter, and it is what makes a 6-year panel computable (5 GB peak, ~1 min).
- **Phase 5 (playground):** done. Pure `domain/orders.py` (state machine + fill rules: limit gap fills at the BETTER open, a stop gapped through fills at the WORSE open, stop beats target in one candle, an order can only fill on candles starting at/after it was placed), `domain/positions.py` (average cost excluding charges; splits conserve total cost and drop-and-write-off fractions; no shorting), `domain/xirr.py` (None, never 0, when undefined). `stk/playground/`: cash is ALWAYS the last `cash_ledger.balance_after` (no second copy); every fill is one transaction and records `fill_basis` (`delayed_intraday` | `eod_fallback`), reason and feed lag; the DP charge is once per scrip per day; bracket children are stamped with the parent candle's END; corporate actions are idempotent on the action's ECONOMIC identity (NSE republishes rows under new hashes) and must run BEFORE the EOD pass. `poller.py`: raw bytes first, judged per symbol, stale feed => park as `pending_eod` and fill NOTHING; every poll is a `poller_runs` row. Intraday candles come from `providers/yfinance/intraday.py` behind its OWN switch `providers.enable_yfinance_intraday` -- independent of `enable_yfinance_fallback`, so it cannot reach the EOD path. Migration `0006_playground`. `stk playground poll|eod|portfolio create|list`. **Deliberate limit:** an order placed mid-session with the feed down waits for the NEXT session's bar (the day's high/low would include price action from before it existed).
- **Phase 6 (AI evening review):** done, **but never exercised against the live API** (no key was available; every test uses a fake `LlmClient`). `stk/ai/`: `client.py` (`LlmClient` protocol + `AnthropicClient`; the SDK is imported nowhere else -- an import-linter contract enforces it, and nothing in `stk.api` imports `stk.ai`, so the dashboard can only READ stored briefs and can never trigger a call), `call_structured` (prompt-driven JSON, fences stripped, pydantic-validated, **exactly one retry** with the errors shown to the model, never raises), `evening.py` (semantic checks a schema can't express: every pick_id/symbol must have been in the input, ranks a clean 1..n, every pick ranked once -- an invented ticker is retried then rejected, never stored), `inputs.py` (DB-only), `prompts.py`. Provider and model are `config/ai.yaml`: `provider: groq` (default; `GroqClient`, plain httpx, JSON mode, `openai/gpt-oss-120b`) or `anthropic` (`claude-sonnet-5` per the brief; adaptive thinking, no sampling params); `make_client` picks one, `stk ai models` checks the Groq key, and `max_input_chars` refuses an over-budget prompt without calling. **Never blocks the pipeline**: any failure is an `ai_runs` row (`failed`/`invalid_output`) and the brief shows "pending". Idempotent per day (a success is not repeated without `--force`); no picks => no call. Cost is an *estimate* from config prices, and `NULL` (not 0) for an unpriced model. Migration `0007_ai`. `stk ai evening [--dry-run] [--force]`, `stk ai usage`. **To verify for real:** `stk ai evening --dry-run` shows exactly what would be sent; then run once with `ANTHROPIC_API_KEY` set.
- **Phase 7 (weekly AI strategy lab):** done, **also never exercised against the live API**. `stk/ai/lab.py`: one model call per week returns proposals as **DSL JSON strings** (never code) plus demotions. Each idea is independent: parse -> strict schema -> semantic validator (unknown indicator, price-vs-literal, hold days vs horizon, slug collision, grid <= 8) -> register (`origin=ai`) -> walk-forward -> gate -> a stored proposal. `keep_on_semantic_failure` means one bad spec doesn't discard its good siblings. **A proposal changes nothing on its own**: `strategies/proposals.py` (deliberately outside `stk.ai`, so the API can decide proposals without being able to call a model) -- approving a NEW strategy requires a passing gate (no exceptions, including for a person), approving a DEMOTION needs `confirm=true`, dismissing a demotion leaves the strategy alone. Migration `0008_lab`; `stk ai lab [--dry-run]`; `/api/proposals` + approve/dismiss; the Strategy lab shows them. **Fixes found along the way:** `transaction()` now NESTS via savepoints (approving composed two transactional helpers and raised "cannot start a transaction within a transaction"); `set_status` refreshes the *reason* on a same-status update (a candidate the gate still couldn't decide kept saying "registered, not yet gated"); the gate treats **too few trades as `insufficient_evidence`, not `fail`** -- the long-term seed made 0 trades because fundamentals aren't ingested, and calling an untested strategy "rejected" was wrong.
- **Data-pipeline fixes from running a real 5-year backfill** (none reachable by the synthetic tests -- worth remembering when tempted to trust them alone): the parquet `series` dictionary index was int8 (max 127 distinct series; NSE has more, the 2023 partition hit 126 and every further write crashed) -> int16, and the writer reads a partition with its OWN schema then casts, so existing int8 partitions stay extendable; one bad `delivery_qty > volume` row (WTICAB, 0.2% over) aborted a whole trading day -> now aborts only if it is systemic (> min(5, 0.5% of rows)); NSE serves the *previous* day's file on holidays and once an `.xlsx` under a `.csv` URL (2022-08-08, a permanent gap, correctly refused); corporate-action parser v2 (see docs/data-sources.md). **A wrapper script that echoes "DONE" without checking the exit code hid the crash for ~an hour -- always check `$?`.**
- **Phase 8 (deploy & hardening):** done as artifacts — **never run on a VM** (none exists). `stk backup run|verify|list|restore` (`store/backup.py`): consistent SQLite snapshot + parquet tarball, checksummed `MANIFEST.json`, **verified after writing**, a destination inside `data/` refused, rotation touches only date-named directories that contain a manifest, restore verifies first and **never deletes** (moves aside to `*.pre-restore-<ts>`). `stk nightly` / `stk weekly` (`cli/orchestrator.py`): every step is its own `stk` subprocess and `job_runs` row (`nightly.<step>`); a failed step blocks only its dependents, which are recorded as failed ("not run: prices failed first"), never omitted. `/api/status` gained `jobAlerts` and the red banner shows them. `stk doctor` gained AI-failure-streak, poller and (`--backup-dest`) backup-age checks. `deploy/` holds the systemd units + timers (Asia/Kolkata), Caddyfile (CSP allows only the TradingView embed), `backup.sh`/`restore.sh`/`install.sh`, `env.example`; `tests/unit/test_deploy.py` checks them against the real CLI. `docs/runbook.md` is the operating guide. aarch64 wheels for the whole lock file were confirmed via `uv pip compile --python-platform aarch64-manylinux_2_28 --only-binary :all:`. `providers/broker/kite.py` is a documented stub.

**All eight phases are built. What is NOT verified, so nobody assumes it is:** the AI evening review and strategy lab against the live API (no key); yfinance intraday during market hours; a full `fundamentals-sweep` + `xbrl` run (so the long-term strategy has never traded); TradingView for BSE-only symbols; most of `config/costs.yaml` (only NSE's 2026-03-01 change is verified); the deploy artifacts on a real VM. **First real promotion results (5-year NSE lake):** none of the ten seed strategies passed the gate: nine were `rejected` (best: `momentum_near_52w_high`, beating the Nifty 500 after costs in 4 of 7 out-of-sample windows vs the 60% required; several beat it in 0-1 of 7) and the long-term one is `insufficient_evidence` (no fundamentals ingested yet). Run over 2021-01-01..2026-09-18 on NSE with the default costs/slippage. That is the gate working, not a bug; do not loosen `config/promotion.yaml` to get a green result.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
